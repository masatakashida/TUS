<%= form_with(model: testreplypost, local: true) do |form| %>
  <% if testreplypost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testreplypost.errors.count, "error") %> prohibited this testreplypost from being saved:</h2>

      <ul>
      <% testreplypost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text, id: :testreplypost_text %>
  </div>

  <div class="field">
    <%= form.label :コメントするユーザーID %>
    <%= form.text_field :testpost_id, id: :testreplypost_testpost_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
