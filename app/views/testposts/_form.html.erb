<%= form_with(model: testpost, local: true) do |form| %>
  <% if testpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testpost.errors.count, "error") %> prohibited this testpost from being saved:</h2>

      <ul>
      <% testpost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text, id: :testpost_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
